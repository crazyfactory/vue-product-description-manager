<div id="app" v-bind:class="editorLanguage">
    <!-- show active messages -->
    <div class="container messages" v-cloak>
        <div v-bind:class="'alert alert-' + message.type" v-for="message in messages_to_show">
            <i class="date">[[message.date]]:</i>
            <span>[[message.message]]</span>
            <i class="fa fa-eye-slash fa-pull-right" aria-hidden="true" @click="hideMessage(message)"></i>
        </div>
    </div>
    <!-- end active messages -->
    <!-- Settings button -->
    <div class="container">
        <i class="fa fa-cogs fa-2x fa-pull-right" aria-hidden="true"
           title="Set languages for Editor and Preview features" @click="toggle_settings"></i>
    </div>
    <!-- end settings button -->
    <!-- settings section -->
    <div class="container settings" v-show="show_settings">
        <ul class="list-group">
            <li class="list-group-item align-right">
                <section class="settings language_editor">
                    <span class="icon_label"><i class="fa fa-pencil fa-2x" aria-hidden="true"
                                                title="Set Editor language"></i></span>
                    <span v-for="language in settings.supportedLanguages">
            <span class="language" @click="addEditorLanguage(language.id)"><i style="color:darkgreen;"
                                                                              class="fa fa-caret-right"
                                                                              aria-hidden="true"
                                                                              v-if="language.id == editorLanguage"></i>
              <span v-bind:class="'flag-icon '+language.flag"></span><i style="color:darkgreen;"
                                                                        class="fa fa-caret-left" aria-hidden="true"
                                                                        v-if="language.id == editorLanguage"></i>
            </span>
          </span>
                </section>
                <span class="settings seperator">|</span>
                <section class="settings language_preview">
                    <span class="icon_label"><i class="fa fa fa-eye fa-2x" aria-hidden="true"
                                                title="Set/ unset languages for the preview feature"></i></span>
                    <span v-for="language in settings.supportedLanguages">
            <span class="language" @click="toggle_language(language)"><i class="fa fa-circle-o" aria-hidden="true"
                                                                         v-if="!language.status"></i>
              <i class="fa fa-check-circle-o" aria-hidden="true" v-if="language.status"></i>
              <span v-bind:class="'flag-icon '+language.flag"></span>
            </span>
          </span>
                </section>
            </li>
        </ul>
    </div>
    <!-- end settings section -->
    <div class="container">
        <h2 class="headline"><span v-bind:class="headline_icon"></span> [[headline]]</h2>
        <div v-if="show_load"
             v-bind:class="{ 'col-xs-12 col-sm-12 col-md-12': isFullScreen, 'col-xs-12 col-sm-6 col-md-6':isSmallScreen }">
            <section class="product-management load_products" @click.stop>
                <!-- Add Products -->
                <header class="header">
                    <div class="margin-bottom">
                        <input class="new-product"
                               autofocus autocomplete="off"
                               placeholder="Enter products model code"
                               v-model="newProduct"
                               @keyup.enter="addProduct">
                        <button class="fa fa-arrow-left switch_actionbar btn btn-default cursor" title="Load Product"
                                aria-hidden="true" @click="addProduct()"></button>
                    </div>
                    <div class="actionbar" v-if="hasProducts">
                        <span class="action"><i>Select:</i></span>
                        <section v-if="!hasEditableProducts">
                            <span title="Nothing to SELECT" class="action"><i style="color:#ccc;"
                                                                              class="fa fa-check-circle-o"
                                                                              aria-hidden="true"></i></span>
                            <span title="Nothing to INVERT" class="action"><i style="color:#ccc;" class="fa fa-random"
                                                                              aria-hidden="true"></i></span>
                            <span title="Nothing to DESELECT" class="action"><i style="color:#ccc;"
                                                                                class="fa fa-circle-o"
                                                                                aria-hidden="true"></i></span>
                        </section>
                        <section v-if="hasEditableProducts">
                            <span title="SELECT ALL products" class="action cursor" @click="selectAllProducts(true)"><i
                                        class="fa fa-check-circle-o" aria-hidden="true"></i></span>
                            <span title="INVERT current selection" class="action cursor"
                                  @click="invertSelectedlProducts()"><i style="color:gold;" class="fa fa-random"
                                                                        aria-hidden="true"></i></span>
                            <span title="DESELECT ALL" class="action cursor" @click="selectAllProducts(false)"><i
                                        class="fa fa-circle-o" aria-hidden="true"></i></span>
                        </section>
                        <span> | </span>
                        <span class="actions"><i>Actions:</i></span>
                        <section v-if="!hasEditableProducts">
                            <span title="Nothing to DELETE" class="action"><i style="color:#ccc;" class="fa fa-trash-o"
                                                                              aria-hidden="true"></i></span>
                            <span title="Nothing to HIDE" class="action"><i style="color:#ccc;" class="fa fa-eye-slash"
                                                                            aria-hidden="true"></i></span>
                        </section>
                        <section v-if="hasEditableProducts">
                            <span title="Discard changes and remove from view" class="action cursor"
                                  @click="removeSeletedProducts()"><i style="color:darkred;" class="fa fa-trash-o"
                                                                      aria-hidden="true"></i></span>
                            <span title="HIDE selected products from view" class="action cursor"
                                  @click="hideSelectedProducts()"><i style="color:darkred;" class="fa fa-eye-slash"
                                                                     aria-hidden="true"></i></span>
                        </section>
                        <span v-if="hasHiddenProducts" title="SHOW all hidden products" class="action cursor"
                              @click="showAllProducts()"><i style="color:darkgreen;" class="fa fa-eye"
                                                            aria-hidden="true"></i></span>
                        <section v-if="show_preview">
                            <span> | </span>
                            <span><i>Filter:</i></span>
                            <span title="Filter names" class="action cursor" @click="switchMe('preview_filter_name')">
                <i class="fa"
                   v-bind:class="{ 'fa-check-circle-o': preview_filter_name, 'fa-circle-o': !preview_filter_name}"
                   aria-hidden="true"></i> Names</span>
                            <span title="Filter descriptions" class="action cursor"
                                  @click="switchMe('preview_filter_description')">
                <i class="fa"
                   v-bind:class="{ 'fa-check-circle-o': preview_filter_description, 'fa-circle-o': !preview_filter_description}"
                   aria-hidden="true"></i> Descriptions</span>
                            <span title="Filter materials" class="action cursor"
                                  @click="switchMe('preview_filter_material')">
                <i class="fa"
                   v-bind:class="{ 'fa-check-circle-o': preview_filter_material, 'fa-circle-o': !preview_filter_material}"
                   aria-hidden="true"></i> Materials</span>
                            <span title="Filter metatags" class="action cursor"
                                  @click="switchMe('preview_filter_metatag')">
                <i class="fa"
                   v-bind:class="{ 'fa-check-circle-o': preview_filter_metatag, 'fa-circle-o': !preview_filter_metatag}"
                   aria-hidden="true"></i> Metatags</span>
                        </section>
                    </div>
                </header>
                <!-- End add products -->
                <section class="main" v-show="products.length" v-cloak>
                    <!-- List products -->
                    <ul class="product-list">
                        <li v-for="product in products_visible"
                            class="product"
                            :key="product.id"
                            :class="{ active: product.active }">
                            <div class="view">
                                <input class="toggle" type="checkbox" v-model="product.active">
                                <i class="product_action fa fa-trash-o fa-2x fa-pull-right cursor text-danger"
                                   aria-hidden="true" @click="removeProduct(product)"></i>
                                <i class="product_action fa fa-eye-slash fa-2x fa-pull-right cursor text-muted"
                                   aria-hidden="true" @click="hideProduct(product)"></i>
                                <label class="white-space">[[product.modelCode]]
                                    <i class="fa fa-floppy-o cursor" title="'Save '+ product.modelCode +' to database'"
                                       aria-hidden="true" @click="exportProduct(product)" v-if="product.dirty"></i>
                                    <span v-if="product.productImage" class="product_image">
                    <!-- ProductImage -->
                    <a v-bind:href="product.productImage" target="_blank"><img v-bind:src="product.productImage"
                                                                               class="img-rounded" width="50"></a>
                  </span>
                                </label>
                                <!-- Generic Product Names by language -->
                                <ul v-if="show_names && (product.base_product || product.component1 || product.component2 || product.cached_names)"
                                    class="list-group indent">
                                    <li class="list-group-item small" v-for="language in settings.supportedLanguages"
                                        v-show="language.status">
                                        <span v-bind:class="'flag-icon '+language.flag"
                                              @click="debugMe(product)"></span>
                                        <span v-if="product.base_product || product.component1 || product.component2">
                      <i class="fa fa-trash-o fa-pull-right cursor" title="Remove this Product name" aria-hidden="true"
                         @click="removeProductName(product)"></i>
                      <i class="fa fa-pencil fa-pull-right cursor" title="Add custom name" aria-hidden="true"
                         v-show="product.names[language.id] && !product.names[language.id].edit"
                         @click="editMe(product.names[language.id], product)"></i>
                      <span class="cursor" @click="editMe(product.names[language.id], product)"
                            v-if="product.names[language.id] && !(product.cached_names && product.cached_names[language.id].value==product.names[language.id].value)">
                        [[product.names[language.id].original_value ]]
                      </span>
                      <a class="fa fa-pull-right cursor" v-if="product.detailsLink" target="_blank"
                         v-bind:href="product.detailsLink +'&lang='+language.id"
                         v-bind:title="'Inspect product '+ product.modelCode +' for language '+ language.id"
                         aria-hidden="true">
                        <i class="product_action fa fa-rocket cursor text-info" aria-hidden="true"></i></a>
                      <div class="customized_name"
                           v-if="product.names[language.id] && ((product.names[language.id].original_value!=product.names[language.id].value) || product.names[language.id].edit)">
                        <i class="fa fa-times-circle-o fa-pull-right cursor" title="Close Editor" aria-hidden="true"
                           v-show="product.names[language.id] && product.names[language.id].edit"
                           @click="closeEditMe(product.names[language.id])"></i>
                        <span v-if="product.names[language.id] && !product.names[language.id].edit"
                              @click="editMe(product.names[language.id], product)"
                              class="fa fa-pencil-square-o fa-pull-left customized_name_icon cursor"
                              v-bind:title="'custom name for '+ language.id"></span>
                        <span v-if="product.names[language.id] && !product.names[language.id].edit" class="cursor"
                              @click="editMe(product.names[language.id], product)">[[ product.names[language.id].value ]]</span>
                        <div class="editable"
                             v-if="product.names && product.names[language.id] && product.names[language.id].edit">
                          <textarea v-if="product.names[language.id] && product.names[language.id].value"
                                    v-model="product.names[language.id].value"
                                    @keyup.esc="closeEditMe(product.names[language.id])"
                                    @keyup.enter="closeEditMe(product.names[language.id])"></textarea>
                        </div>
                        <i class="fa fa-trash-o fa-pull-right cursor" aria-hidden="true"
                           v-bind:title="'Delete custom name for '+language.id"
                           @click="removeCustomName(product.names[language.id])"></i>
                      </div>
                    </span>
                                        <span class="cached"
                                              v-if="product.cached_names && product.cached_names[language.id]"
                                              v-bind:class="{ 'override': ((product.base_product || product.component1 || product.component2) && (product.names[language.id] && product.cached_names[language.id] && product.cached_names[language.id].value != product.names[language.id].value))}">
                      [[ product.cached_names[language.id].value ]] <br>
                    </span>
                                    </li>
                                </ul>
                                <!-- End Product Names by language -->
                                <div class="indent" v-if="show_material">
                                    <!-- material configuration -->
                                    <ul class="list-group"
                                        v-if="product.materials.length >0 || product.cached_materials">
                                        <li class="list-group-item small"
                                            v-for="language in settings.supportedLanguages" v-show="language.status">
                                            <span v-bind:class="'flag-icon cursor '+language.flag"
                                                  @click="debugMe(product)"></span>
                                            <span v-for="(material, index) in product.materials">
                        <span v-if="index > 0 && material">/</span>
                        <span v-if="material[language.id] && material[language.id]!== undefined">[[ material[language.id] ]]</span>
                      </span>
                                            <i class="fa fa-pull-right fa-trash-o cursor"
                                               @click="removeMaterial(product);"></i>
                                            <a class="product_action fa fa-pull-right cursor" v-if="product.detailsLink"
                                               target="_blank" v-bind:href="product.detailsLink +'&lang='+language.id"
                                               v-bind:title="'Inspect product '+ product.modelCode +' for language '+ language.id"
                                               aria-hidden="true">
                                                <i class="product_action fa fa-rocket cursor text-info"
                                                   aria-hidden="true"></i>
                                            </a>
                                            <span v-if="product.cached_materials && product.cached_materials[language.id]"
                                                  class="cached"
                                                  v-bind:class="{ 'override': product.materials && product.materials.length >0 }">
                                                [[ product.cached_materials[language.id].value ]]</span>
                                        </li>
                                    </ul>
                                    <!-- end material -->
                                </div>
                                <div v-if="show_metatags">
                                    <!-- Metatag preview -->
                                    <ul class="indent"
                                        v-if="(product.metatags && product.metatags.length>0) || (product.cached_metatags)">
                                        <li class="list-group-item small"
                                            v-for="language in settings.supportedLanguages" v-show="language.status">
                                            <ul>
                                                <li v-bind:class="{'no-bottom-space': product.cached_metatags && product.cached_metatags[language.id] }">
                                                    <span v-bind:class="'flag-icon fa-pull-left cursor '+language.flag"
                                                          @click="debugMe(product)"
                                                          style="margin-top:-8px; margin-left:-12px;"></span>
                                                    <span v-if="product.cached_metatags && product.cached_metatags[language.id]"
                                                          class="cached metatags"
                                                          v-bind:class="{ 'override': (product.metatags && product.metatags.length>0) }">
                                                        [[ product.cached_metatags[language.id].value ]]</span>
                                                </li>
                                                <li class="list-group-item" v-for="metatag in product.metatags">
                                                    <i class="fa fa-trash-o fa-pull-right cursor" aria-hidden="true"
                                                       title="Delete this tag"
                                                       @click="removeMetatag(product, metatag)"></i>
                                                    <span v-if="product.metatags ">
                                                        <i class="fa fa-tag fa-pull-left"></i>
                                                        <i class="fa fa-eye-slash fa-pull-left"
                                                           v-if="metatag.is_hidden>0"></i>
                                                        [[ metatag[language.id] ]]
                                                    </span>
                                                    <span class="alias"
                                                          v-if='metatag && metatag["alias_" +language.id]'>
                            <i class="fa fa-tags"></i>[[  metatag["alias_" +language.id] ]]
                          </span>
                                                </li>
                                            </ul>
                                        </li>
                                    </ul>
                                    <!-- Meta tag preview end -->
                                </div>
                                <!-- Preview Feature -->
                                <div class="product_preview" v-on:click.stop v-if="show_preview">
                                    <div class="indent" v-if="preview_filter_name">
                                        <h5 class="product_name inline" v-if="product.base_product"><i
                                                    class="fa fa-commenting-o fa-pull-left" aria-hidden="true"></i>
                                            Names:</h5>
                                        <ul class="list-group" v-if="product.base_product">
                                            <li class="list-group-item small"
                                                v-for="language in settings.supportedLanguages"
                                                v-show="language.status">
                                                <i class="fa fa-pencil fa-pull-right cursor" title="Add custom name"
                                                   aria-hidden="true"
                                                   v-show="product.names[language.id] && !product.names[language.id].edit"
                                                   @click="editMe(product.names[language.id], product)"></i>
                                                <i class="fa fa-times-circle-o fa-pull-right cursor"
                                                   title="Close Editor" aria-hidden="true"
                                                   v-show="product.names[language.id] && product.names[language.id].edit"
                                                   @click="closeEditMe(product.names[language.id])"></i>
                                                <span v-bind:class="'flag-icon '+language.flag"></span>
                                                <a class="product_action fa fa-pull-right cursor"
                                                   v-if="product.detailsLink" target="_blank"
                                                   v-bind:href="product.detailsLink +'&lang='+language.id"
                                                   v-bind:title="'Inspect product '+ product.modelCode +' for language '+ language.id"
                                                   aria-hidden="true">
                                                    <i class="product_action fa fa-rocket cursor text-info"
                                                       aria-hidden="true"></i>
                                                </a>
                                                <span v-if="product.names[language.id] && !product.names[language.id].edit"
                                                      class="cursor"
                                                      @click="editMe(product.names[language.id], product)">[[ product.names[language.id].value ]]</span>
                                                <div class="editable"
                                                     v-if="product.names && product.names[language.id] && product.names[language.id].edit">
                                                    <textarea
                                                            v-if="product.names[language.id] && product.names[language.id].value"
                                                            v-model="product.names[language.id].value"
                                                            @keyup.enter="closeEditMe(product.names[language.id])"
                                                            @keyup.esc="closeEditMe(product.names[language.id])"></textarea>
                                                </div>
                                                <span class="cached"
                                                      v-if="product.cached_names && product.cached_names[language.id]"
                                                      v-bind:class="{ 'override': product.names[language.id] && (product.base_product || product.component1 || product.component2)}">
                                                    [[product.cached_names[language.id].value ]] <br>
                                                </span>
                                            </li>
                                        </ul>
                                    </div>
                                    <div v-if="product.descriptions && preview_filter_description"
                                         class="indent description">
                                        <h5><i class="fa fa-info-circle fa-pull-left" aria-hidden="true"></i>
                                            Descriptions:</h5>
                                        <ul class="list-group">
                                            <!-- Language dependent items -->
                                            <li class="list-group-item small"
                                                v-for="language in settings.supportedLanguages"
                                                v-show="language.status">

                                                <i class="fa fa-pencil fa-pull-right cursor"
                                                   title="Add custom description" aria-hidden="true"
                                                   v-show="product.descriptions[language.id] && !product.descriptions[language.id].edit"
                                                   @click="editMe(product.descriptions[language.id], product)"></i>
                                                <i class="fa fa-times-circle-o fa-pull-right cursor"
                                                   title="Close Editor" aria-hidden="true"
                                                   v-show="product.descriptions[language.id] && product.descriptions[language.id].edit"
                                                   @click="closeEditMe(product.descriptions[language.id])"></i>

                                                <i class="fa fa-refresh fa-pull-right cursor"
                                                   title="Generate new description" aria-hidden="true"
                                                   @click="getGeneratedDescription(product, language.id)"
                                                   v-if="languages_autodescription.indexOf(language.id)>-1"></i>

                                                <span v-bind:class="'flag-icon '+language.flag"></span>
                                                <a class="product_action fa fa-pull-right cursor"
                                                   v-if="product.detailsLink" target="_blank"
                                                   v-bind:href="product.detailsLink +'&lang='+language.id"
                                                   v-bind:title="'Inspect product '+ product.modelCode +' for language '+ language.id"
                                                   aria-hidden="true">
                                                    <i class="product_action fa fa-rocket cursor text-info"
                                                       aria-hidden="true"></i>
                                                </a>
                                                <!-- original description -->

                                                <span v-if="product.cached_descriptions && product.cached_descriptions[language.id] && product.descriptions[language.id]"
                                                      class="cached"
                                                      v-bind:class="{ 'override': !(product.descriptions[language.id].value == product.cached_descriptions[language.id].value) }">
                                                    [[ product.cached_descriptions[language.id].value ]]</span>
                                                <!-- end original description -->
                                                <span v-if="!product.cached_descriptions || ((product.descriptions[language.id] && product.cached_descriptions[language.id]) && !(product.descriptions[language.id].value == product.cached_descriptions[language.id].value))"
                                                      class="cursor"
                                                      @click="editMe(product.descriptions[language.id],product)">
                                                    [[product.descriptions[language.id].value ]]</span>
                                                <div class="editable" v-if="product.descriptions[language.id]"
                                                     v-show="product.descriptions[language.id].edit">
                                                    <textarea v-model="product.descriptions[language.id].value"
                                                              @keyup.enter="closeEditMe(product.descriptions[language.id])"></textarea>
                                                </div>

                                            </li>
                                            <!-- end language dependent items -->
                                        </ul>
                                    </div>
                                    <div v-if="(product.materials.length>0 || product.cached_materials) && preview_filter_material"
                                         class="indent">
                                        <h5><i class="fa fa-industry fa-pull-left" aria-hidden="true"></i> Materials:
                                        </h5>
                                        <!-- Material Preview -->
                                        <ul class="list-group">
                                            <li class="list-group-item small"
                                                v-for="language in settings.supportedLanguages"
                                                v-show="language.status">
                                                <span v-bind:class="'flag-icon cursor '+language.flag"
                                                      @click="debugMe(product)"></span>
                                                <span v-for="(material, index) in product.materials">
                                                    <span v-if="index > 0">/</span>
                                                    <span v-if="material[language.id]">[[ material[language.id] ]]</span>
                                                </span>
                                                <i class="fa fa-pull-right fa-trash-o cursor"
                                                   @click="removeMaterial(product);"></i>
                                                <span v-if="product.cached_materials && product.cached_materials[language.id]"
                                                      class="cached"
                                                      v-bind:class="{ 'override': product.materials.length >0 }">
                                                [[ product.cached_materials[language.id].value ]]</span>
                                                <a class="product_action fa fa-pull-right cursor"
                                                   v-if="product.detailsLink" target="_blank"
                                                   v-bind:href="product.detailsLink +'&lang='+language.id"
                                                   v-bind:title="'Inspect product '+ product.modelCode +' for language '+ language.id"
                                                   aria-hidden="true">
                                                    <i class="product_action fa fa-rocket cursor text-info"
                                                       aria-hidden="true"></i>
                                                </a>
                                            </li>
                                        </ul>
                                        <!-- End Material Preview -->
                                    </div>

                                    <div v-if="preview_filter_metatag && (product.cached_metatags || (product.metatags && product.metatags.length>0))">
                                        <h5 class="indent"><i class="fa fa-tags fa-pull-left" aria-hidden="true"></i>
                                            Metatags:</h5>
                                        <!-- Meta tag preview -->
                                        <ul class="indent"
                                            v-if="product.cached_metatags || (product.metatags && product.metatags.length>0)">
                                            <li class="list-group-item small"
                                                v-for="language in settings.supportedLanguages"
                                                v-show="language.status">
                                                <ul>
                                                    <li v-bind:class="{'no-bottom-space': product.cached_metatags }">
                                                        <span v-bind:class="'flag-icon fa-pull-left cursor '+language.flag"
                                                              @click="debugMe(product)"
                                                              style="margin-top:-8px; margin-left:-12px;"></span>
                                                        <span v-if="product.cached_metatags && product.cached_metatags[language.id]"
                                                              class="cached metatags"
                                                              v-bind:class="{ 'override': (product.metatags && product.metatags.length>0)}">
                              [[ product.cached_metatags[language.id].value ]]
                            </span>
                                                    </li>
                                                    <li class="list-group-item" v-for="metatag in product.metatags">
                            <span v-if="metatag"><i class="fa fa-tag fa-pull-left"></i>
                              <span v-if="metatag.invisible" class="fa fa-minus-circle" aria-hidden="true"></span>
                              [[ metatag[language.id] ]]
                            </span>
                                                        <span class="alias"
                                                              v-if='metatag && metatag["alias_" +language.id]'>
                              <i class="fa fa-tags"></i>[[  metatag["alias_" +language.id] ]]
                            </span>
                                                        <i class="fa fa-trash-o fa-pull-right cursor" aria-hidden="true"
                                                           title="Delete this tag"
                                                           @click="removeMetatag(product, metatag)"></i>
                                                    </li>
                                                </ul>
                                            </li>
                                        </ul>
                                        <!-- Meta tag preview end-->
                                    </div>

                                </div>
                                <!-- Preview END -->
                            </div>
                        </li>
                    </ul>
                    <!-- End List products -->
                </section>
            </section>
        </div>
        <!-- Add Products end -->
        <div v-bind:class="{ 'col-xs-12 col-sm-12 col-md-12': isFullScreen, 'col-xs-12 col-sm-6 col-md-6':isSmallScreen }"
             @click.stop v-if="show_translations">
            <!-- Translation stuff -->
            <div v-if="dirtyTranslations.isDirty" class="alert alert-warning">
                <b>Warning: unsaved changes!</b><br>
                <i>Better save then sorry. You updated translations but didn't stored them yet.</i><br><br>
                <b>Your options are:</b>
                <ul>
                    <li v-if="dirtyTranslations.baseProducts.isDirty">
                        <i class="fa fa-floppy-o cursor" title="Save BaseProduct translations to database"
                           aria-hidden="true" @click="saveTranslationUpdates('baseProducts')"
                           v-if="dirtyTranslations.baseProducts.isDirty">&nbsp;Save Base Products</i>
                    </li>
                    <li v-if="dirtyTranslations.components.isDirty">
                        <i class="fa fa-floppy-o cursor" title="Save Component translations to database"
                           aria-hidden="true" @click="saveTranslationUpdates('components')"
                           v-if="dirtyTranslations.components.isDirty">&nbsp;Save Components</i>
                    </li>
                    <li v-if="dirtyTranslations.materials.isDirty">
                        <i class="fa fa-floppy-o cursor" title="Save Material translations to database"
                           aria-hidden="true" @click="saveTranslationUpdates('materials')"
                           v-if="dirtyTranslations.materials.isDirty">&nbsp;Save Materials</i>
                    </li>
                    <li v-if="dirtyTranslations.metatags.isDirty">
                        <i class="fa fa-floppy-o cursor" title="Save Metatag translations to database"
                           aria-hidden="true" @click="saveTranslationUpdates('metatags')"
                           v-if="dirtyTranslations.metatags.isDirty">&nbsp;Save Metatags</i>
                    </li>
                </ul>
            </div>
            <div>
                <h4 class="base_products">
                    <i v-if="!show_translation_base_products" @click="toggleTranslationVisibility('base_products')"
                       class="fa fa-plus-square-o" aria-hidden="true"></i>
                    <i v-if="show_translation_base_products" @click="toggleTranslationVisibility('base_products')"
                       class="fa fa-minus-square-o" aria-hidden="true"></i>
                    Base Products <i class="fa fa-floppy-o cursor" title="Save BaseProduct translations to database"
                                     aria-hidden="true" @click="saveTranslationUpdates('baseProducts')"
                                     v-if="dirtyTranslations.baseProducts.isDirty"></i>
                </h4>
                <p v-if="show_translation_base_products" class="translation_table base_products">
                    <datatable
                            :source="translationsBaseProducts"
                            :editable="true"
                            :threshold="table.threshold">
                        <datatable-column id="actions_baseproducts" label="Actions" :sortable="false"
                                          :groupable="false">&nbsp;
                        </datatable-column>
                        <datatable-column v-for="column in activeLanguages" :key="column.id"
                                          :id="column.id"
                                          :label="column.label"
                                          :width="column.width"
                                          :sortable="column.sortable"
                                          :groupable="column.groupable"
                                          :aggregators="column.aggregators"
                                          :formatter="column.formatter">
                        </datatable-column>

                        <template slot="actions_baseproducts" scope="cell">
                            <div class="right-distance">
                                <span @click="switchTranslationStatus('baseProducts', cell)">
                                    <i v-if="cell.row['translation_requested'] == '1'" class="request fa fa-bell"
                                       aria-hidden="true"></i>
                                    <i v-else class="complete-request fa fa-check-circle" aria-hidden="true"></i>
                                </span>
                            </div>
                        </template>
                        <template slot="de" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-de"></span>
                                <input class="table_input" v-model:value="cell.row['de']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="en-GB" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-gb"></span>
                                <input class="table_input" v-model:value="cell.row['en-GB']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="en-US" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-us"></span>
                                <input class="table_input" v-model:value="cell.row['en-US']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="es" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-es"></span>
                                <input class="table_input" v-model:value="cell.row['es']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="fr" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-fr"></span>
                                <input class="table_input" v-model:value="cell.row['fr']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="it" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-it"></span>
                                <input class="table_input" v-model:value="cell.row['it']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="nl" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-nl"></span>
                                <input class="table_input" v-model:value="cell.row['nl']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="pt" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-pt"></span>
                                <input class="table_input" v-model:value="cell.row['pt']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="ru" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-ru"></span>
                                <input class="table_input" v-model:value="cell.row['ru']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="sv" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-se"></span>
                                <input class="table_input" v-model:value="cell.row['sv']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="cs" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-cz"></span>
                                <input class="table_input" v-model:value="cell.row['cs']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="fi" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-fi"></span>
                                <input class="table_input" v-model:value="cell.row['fi']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="hr" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-hr"></span>
                                <input class="table_input" v-model:value="cell.row['hr']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="nb" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-no"></span>
                                <input class="table_input" v-model:value="cell.row['nb']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>
                        <template slot="pl" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-pl"></span>
                                <input class="table_input" v-model:value="cell.row['pl']" type="text"
                                       @input="stashUpdate('baseProducts', cell)"
                                       @keyup.enter="saveTranslationUpdate('baseProducts', cell)"/></div>
                        </template>

                    </datatable>
                </p>
            </div>
            <div>
                <h4 class="components">
                    <i v-if="!show_translation_components" @click="toggleTranslationVisibility('components')"
                       class="fa fa-plus-square-o" aria-hidden="true"></i>
                    <i v-if="show_translation_components" @click="toggleTranslationVisibility('components')"
                       class="fa fa-minus-square-o" aria-hidden="true"></i>
                    Components <i class="fa fa-floppy-o cursor" title="Save Component translations to database"
                                  aria-hidden="true" @click="saveTranslationUpdates('components')"
                                  v-if="dirtyTranslations.components.isDirty"></i>
                </h4>
                <p v-if="show_translation_components" class="translation_table components">
                    <datatable
                            :source="translationsComponents"
                            :editable="true"
                            :threshold="table.threshold">
                        <datatable-column id="actions_components" label="Actions" :sortable="false"
                                          :groupable="false"></datatable-column>
                        <datatable-column v-for="column in activeLanguages" :key="column.id"
                                          :id="column.id"
                                          :label="column.label"
                                          :width="column.width"
                                          :sortable="column.sortable"
                                          :groupable="column.groupable"
                                          :aggregators="column.aggregators"
                                          :formatter="column.formatter">
                        </datatable-column>
                        <template slot="actions_components" scope="cell">
                            <div class="right-distance">
                                <span @click="switchTranslationStatus('components', cell)">
                                    <i v-if="cell.row['translation_requested'] == '1'" class="request fa fa-bell"
                                       aria-hidden="true"></i>
                                    <i v-else class="complete-request fa fa-check-circle" aria-hidden="true"></i>
                                </span>
                            </div>
                        </template>
                        <template slot="de" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-de"></span>
                                <input class="table_input" v-model:value="cell.row['de']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="en-GB" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-gb"></span>
                                <input class="table_input" v-model:value="cell.row['en-GB']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="en-US" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-us"></span>
                                <input class="table_input" v-model:value="cell.row['en-US']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="es" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-es"></span>
                                <input class="table_input" v-model:value="cell.row['es']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="fr" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-fr"></span>
                                <input class="table_input" v-model:value="cell.row['fr']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="it" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-it"></span>
                                <input class="table_input" v-model:value="cell.row['it']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="nl" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-nl"></span>
                                <input class="table_input" v-model:value="cell.row['nl']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="pt" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-pt"></span>
                                <input class="table_input" v-model:value="cell.row['pt']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="ru" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-ru"></span>
                                <input class="table_input" v-model:value="cell.row['ru']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="sv" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-se"></span>
                                <input class="table_input" v-model:value="cell.row['sv']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="cs" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-cz"></span>
                                <input class="table_input" v-model:value="cell.row['cs']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="fi" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-fi"></span>
                                <input class="table_input" v-model:value="cell.row['fi']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="hr" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-hr"></span>
                                <input class="table_input" v-model:value="cell.row['hr']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="nb" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-no"></span>
                                <input class="table_input" v-model:value="cell.row['nb']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                        <template slot="pl" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-pl"></span>
                                <input class="table_input" v-model:value="cell.row['pl']" type="text"
                                       @input="stashUpdate('components', cell)"
                                       @keyup.enter="saveTranslationUpdate('components', cell)"/></div>
                        </template>
                    </datatable>
                </p>
            </div>
            <div>
                <h4 class="materials">
                    <i v-if="!show_translation_materials" @click="toggleTranslationVisibility('materials')"
                       class="fa fa-plus-square-o" aria-hidden="true"></i>
                    <i v-if="show_translation_materials" @click="toggleTranslationVisibility('materials')"
                       class="fa fa-minus-square-o" aria-hidden="true"></i>
                    Materials <i class="fa fa-floppy-o cursor" title="Save Material translations to database"
                                 aria-hidden="true" @click="saveTranslationUpdates('materials')"
                                 v-if="dirtyTranslations.materials.isDirty"></i>
                </h4>
                <p v-if="show_translation_materials" class="translation_table materials">
                    <datatable
                            :source="translationsMaterials"
                            :editable="true"
                            :threshold="table.threshold">
                        <datatable-column id="actions_materials" label="Actions" :sortable="false"
                                          :groupable="false"></datatable-column>
                        <datatable-column v-for="column in activeLanguages" :key="column.id"
                                          :id="column.id"
                                          :label="column.label"
                                          :width="column.width"
                                          :sortable="column.sortable"
                                          :groupable="column.groupable"
                                          :aggregators="column.aggregators"
                                          :formatter="column.formatter">
                        </datatable-column>
                        <template slot="actions_materials" scope="cell">
                            <div v-if="cell.row['is_active']=='1'" class="right-distance">
                                <span @click="switchTranslationStatus('materials', cell)">
                                    <i v-if="cell.row['translation_requested'] == '1'" class="request fa fa-bell"
                                       aria-hidden="true"></i>
                                    <i v-else class="complete-request fa fa-check-circle" aria-hidden="true"></i>
                                </span>
                            </div>
                        </template>
                        <template slot="de" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-de"></span>
                                <input class="table_input" v-model:value="cell.row['de']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="en-GB" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-gb"></span>
                                <input class="table_input" v-model:value="cell.row['en-GB']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="en-US" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-us"></span>
                                <input class="table_input" v-model:value="cell.row['en-US']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="es" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-es"></span>
                                <input class="table_input" v-model:value="cell.row['es']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="fr" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-fr"></span>
                                <input class="table_input" v-model:value="cell.row['fr']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="it" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-it"></span>
                                <input class="table_input" v-model:value="cell.row['it']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="nl" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-nl"></span>
                                <input class="table_input" v-model:value="cell.row['nl']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="pt" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-pt"></span>
                                <input class="table_input" v-model:value="cell.row['pt']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="ru" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-ru"></span>
                                <input class="table_input" v-model:value="cell.row['ru']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="sv" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-sv"></span>
                                <input class="table_input" v-model:value="cell.row['sv']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="cs" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-cz"></span>
                                <input class="table_input" v-model:value="cell.row['cs']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="fi" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-fi"></span>
                                <input class="table_input" v-model:value="cell.row['fi']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="hr" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-hr"></span>
                                <input class="table_input" v-model:value="cell.row['hr']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="nb" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-no"></span>
                                <input class="table_input" v-model:value="cell.row['nb']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                        <template slot="pl" scope="cell">
                            <div v-if="cell.row['is_active']=='1'"><span class="absolute flag-icon flag-icon-pl"></span>
                                <input class="table_input" v-model:value="cell.row['pl']" type="text"
                                       @input="stashUpdate('materials', cell)"
                                       @keyup.enter="saveTranslationUpdate('materials', cell)"/></div>
                        </template>
                    </datatable>
                </p>
            </div>
            <div>
                <h4 class="metatags">
                    <i v-if="!show_translation_metatags" @click="toggleTranslationVisibility('metatags')"
                       class="fa fa-plus-square-o" aria-hidden="true"></i>
                    <i v-if="show_translation_metatags" @click="toggleTranslationVisibility('metatags')"
                       class="fa fa-minus-square-o" aria-hidden="true"></i>
                    Metatags <i class="fa fa-floppy-o cursor" title="Save Metatag translations to database"
                                aria-hidden="true" @click="saveTranslationUpdates('metatags')"
                                v-if="dirtyTranslations.metatags.isDirty"></i>
                </h4>
                <p v-if="show_translation_metatags" class="translation_table metatags">
                    <datatable
                            :source="translationsMetatags"
                            :editable="true"
                            :threshold="table.threshold">
                        <datatable-column id="actions_metatags" label="Actions" :sortable="false"
                                          :groupable="false"></datatable-column>
                        <datatable-column v-for="column in activeLanguages" :key="column.id"
                                          :id="column.id"
                                          :label="column.label"
                                          :width="column.width"
                                          :sortable="column.sortable"
                                          :groupable="column.groupable"
                                          :aggregators="column.aggregators"
                                          :formatter="column.formatter">
                        </datatable-column>
                        <template slot="actions_metatags" scope="cell">
                            <div v-if="cell.row['is_active']=='1'" class="right-distance">
                                <span @click="switchTranslationStatus('metatags', cell)">
                                    <i v-if="cell.row['translation_requested'] == '1'" class="request fa fa-bell"
                                       aria-hidden="true"></i>
                                    <i v-else class="complete-request fa fa-check-circle" aria-hidden="true"></i>
                                </span>
                            </div>
                        </template>
                        <template slot="de" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-de"></span>
                                <input class="table_input" v-model:value="cell.row['de']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_de']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="en-GB" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-gb"></span>
                                <input class="table_input" v-model:value="cell.row['en-GB']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_en-GB']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="en-US" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-us"></span>
                                <input class="table_input" v-model:value="cell.row['en-US']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_en-US']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="es" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-es"></span>
                                <input class="table_input" v-model:value="cell.row['es']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_es']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="fr" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-fr"></span>
                                <input class="table_input" v-model:value="cell.row['fr']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_fr']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="it" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-it"></span>
                                <input class="table_input" v-model:value="cell.row['it']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_it']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="nl" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-nl"></span>
                                <input class="table_input" v-model:value="cell.row['nl']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_nl']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="pt" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-pt"></span>
                                <input class="table_input" v-model:value="cell.row['pt']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_pt']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="ru" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-ru"></span>
                                <input class="table_input" v-model:value="cell.row['ru']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_ru']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="sv" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-se"></span>
                                <input class="table_input" v-model:value="cell.row['sv']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_sv']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="cs" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-cz"></span>
                                <input class="table_input" v-model:value="cell.row['cs']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_cs']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="fi" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-fi"></span>
                                <input class="table_input" v-model:value="cell.row['fi']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_fi']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="hr" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-hr"></span>
                                <input class="table_input" v-model:value="cell.row['hr']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_hr']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="nb" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-no"></span>
                                <input class="table_input" v-model:value="cell.row['nb']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_nb']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                        <template slot="pl" scope="cell">
                            <div v-if="cell.row['is_active']=='1'">
                                <span class="absolute flag-icon flag-icon-pl"></span>
                                <input class="table_input" v-model:value="cell.row['pl']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                                <span class="absolute fa fa-tags"></span>
                                <input class="table_input" v-model:value="cell.row['alias_pl']" type="text"
                                       @input="stashUpdate('metatags', cell)"
                                       @keyup.enter="saveTranslationUpdate('metatags', cell)"/>
                            </div>
                        </template>
                    </datatable>
                </p>
            </div>
        </div>
    </div>
</div>
